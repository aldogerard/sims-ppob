import React from 'react';

const App = () => {
    const handleClick = () => {};

    return (
        <section>
            <button onClick={handleClick}>Login</button>
        </section>
    );
};

export default App;
